services:
  - type: web
    name: watch-together
    env: node
    buildCommand: cd frontend && npm run build && cd ../backend && npm install
    startCommand: cd backend && node server.js
    envVars:
      - key: NODE_ENV
        value: production
